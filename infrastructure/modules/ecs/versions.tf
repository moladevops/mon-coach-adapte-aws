# infrastructure/modules/ecs/versions.tf
# VERSIONS TERRAFORM ET PROVIDERS

terraform {
  required_version = ">= 1.0"
  
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}